coverage:
  status:
    project:
      default:
        # basic
        target: 95%
        threshold: 1%
        base: auto
    patch:
      default:
        # basic
        target: auto
        threshold: 1%
        base: auto
