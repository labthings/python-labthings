coverage:
  status:
    project:
      default:
        # basic
        target: auto
        threshold: 5%
        base: auto
    patch:
      default:
        # basic
        target: 80%
        threshold: 5%
        base: auto
